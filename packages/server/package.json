{
  "name": "online-scoreboard-server",
  "version": "0.0.1",
  "description": "Online Scoreboard Server",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git@github.com:Online-Scoreboard/online-scoreboard.git"
  },
  "scripts": {
    "serverless": "serverless",
    "prod:deploy": "serverless --stage=prod --accountId=$ACCOUNT_ID deploy",
    "prod:remove": "serverless --stage=prod remove",
    "stage:deploy": "serverless --stage=stage --accountId=$ACCOUNT_ID deploy",
    "stage:remove": "serverless --stage=stage remove",
    "prod:info": "serverless --stage=prod info",
    "stage:info": "serverless --stage=stage info"
  },
  "devDependencies": {
    "aws-appsync": "^1.0.0",
    "graphql": "^0.11.0",
    "serverless": "^1.48.1",
    "serverless-appsync-offline": "^1.4.0",
    "serverless-appsync-plugin": "^1.1.2",
    "serverless-dynamodb-client": "0.0.2",
    "serverless-dynamodb-local": "^0.2.38",
    "serverless-offline": "^5.12.1"
  },
  "license": "MIT",
  "dependencies": {
    "unique-names-generator": "^4.1.1"
  }
}
